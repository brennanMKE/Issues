# 0157 — Issues.app: add a filter for issues that have attachments

| | |
|---|---|
| **Status** | resolved |
| **Module** | Issues.app |
| **Platform** | macOS |
| **First seen** | 2026-05-07 |
| **Closed** | 2026-05-07 |
| **Commit** | f58a64d |

## Description

When triaging the issue list it's useful to focus on issues that ship attachments (screenshots, crash logs, sample data) — they tend to be visual / regression bugs that benefit from a different review pass than text-only feature requests.

There's no current way to filter the list by attachment presence. Add a filter affordance on the existing filter bar (between the status pills and the module/platform dropdowns, or as another toggle in that row) so the user can scope to "issues with attachments" with a single click.

## Attachments

![Filter bar where the new selector should go](0071/filter-bar.png)

## Steps to reproduce

1. Open Issues.app.
2. Look for a way to filter the list to only issues that have a populated attachment folder.
3. Observe there's none.

## Expected behavior

A filter control on the filter bar with three states (or a tri-state cycle):

- **All issues** (default — no filtering by attachment).
- **With attachments** — show only issues whose `NNNN/` folder exists and contains at least one file.
- **Without attachments** — the inverse, useful for surfacing text-only issues that might benefit from a screenshot.

Concrete shapes that fit the existing bar (any one is acceptable):

- A pill toggle styled like the status pills, e.g. **Has Attachments** (orange dot, tappable on/off).
- A dropdown styled like "All Modules" / "All Platforms", e.g. **All Attachments ▼** with options "All / With attachments / Without attachments".
- A small paperclip icon button that cycles through the three states with a tooltip.

Any of these compose with the existing status / module / platform filters using AND semantics — picking "Open" + "With attachments" shows only open issues that have attachments.

## Actual behavior

No filter control. Reviewers have to click into each issue (or scroll past hundreds) to find the ones with screenshots.

## Implementation notes

- Source of truth: `<issuesRoot>/NNNN/` directory existence + non-empty content. An issue with `NNNN/` present but empty (like the `0060/` folder in the current Bluesky-Migration repo) should count as "without attachments" — the filter should test for *files* inside, not just folder existence.
- Cheap to compute: walk the existing model that already maps each `NNNN.md` to its sibling folder.
- Persist the selected filter in the same place the existing filters persist (UserDefaults keyed off the issues-root path, presumably) so reload doesn't reset it.
- The image in the existing `## Attachments` section of an issue counts even when the file is missing on disk — but for the *filter*, prefer "folder exists and contains at least one regular file" rather than parsing the markdown for `![](X)` references. Filesystem signal is faster and more reliable.

## Notes

- Pairs with the recent attachment-path-resolution clean-up (the parity-audit work in this issues folder revealed a lot of broken refs precisely because there was no easy way to spot which issues had attachments at all).
- Filed in the Bluesky-Migration project per the existing pattern (see #0067), which tracks Issues.app feature work here even though the code lives in `brennanMKE/Issues`.

## Root cause

There was simply no filter affordance for attachment presence. The existing toolbar exposed status pills plus Module / Platform dropdowns; nothing surfaced the sibling `NNNN/` folder content. Reviewers had to click into each issue to discover whether it shipped a screenshot.

## Fix

- Added `Issue.hasAttachments: Bool`, populated once per reload by `MarkdownIssueParser.attachmentFolderHasFiles(for:)`. The helper stats `<issuesRoot>/NNNN/` and returns `true` only when at least one *regular file* lives inside; an empty `NNNN/` counts as no attachments. Stat happens once during parse so filter changes don't re-hit the disk.
- Added `IssueStore.AttachmentFilter` (`all` / `withAttachments` / `withoutAttachments`) defaulting to `.all`, plus the predicate inside `filteredIssues` AND-composed with status / module / platform / search.
- Surfaced the control as a `Picker(.menu)` in `ToolbarView`, styled to match "All Modules" / "All Platforms".
- Persisted via `TabPersistedState.attachmentFilter: String?`. Optional so older persisted blobs decode cleanly; `apply` resolves missing or unknown raws to `.all`.

## Files changed

- `Issues/Models/Issue.swift` — new `hasAttachments` field on the value type.
- `Issues/Services/MarkdownIssueParser.swift` — `parse`/`parseDetailed` accept `hasAttachments`; new `attachmentFolderHasFiles(for:)` helper computed from disk in the disk-based `parse(fileURL:)` overload.
- `Issues/State/IssueStore.swift` — new `AttachmentFilter` enum, `attachmentFilter` field, predicate in `filteredIssues`, and round-trip in `persistedState()` / `apply(_:)`.
- `Issues/State/TabPersistedState.swift` — new optional `attachmentFilter: String?`.
- `Issues/Views/ToolbarView.swift` — third menu picker after Module / Platform.
- `Issues/Views/PreviewSamples.swift`, `Issues/Views/MainView.swift`, `Issues/State/TabsModel.swift` — pass `hasAttachments` to the new initializer.
- `IssuesTests/IssueStoreTests.swift`, `IssuesTests/LintRunnerTests.swift`, `IssuesTests/TabsModelTests.swift` — fixtures pass the new field; persisted-state cases pass `attachmentFilter: nil`.

## Gotchas

- `TabPersistedState.attachmentFilter` is `String?` rather than the enum directly so a downgrade-then-upgrade cycle (or a JSON blob from before this commit) decodes without throwing — same forward-compat pattern as `viewMode` / `sortColumn`.
- The folder stat happens during `parse(fileURL:)`, not in the pure `parse(fileURL:contents:)` overload — that path keeps `hasAttachments` defaulted to `false` so the parser stays disk-free for unit tests.
