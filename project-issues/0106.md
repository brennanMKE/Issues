# 0106 — Async attachment fetch on the viewer

| | |
|---|---|
| **Status** | resolved |
| **Module** | Views / Services |
| **Platform** | macOS |
| **First seen** | 2026-05-08 |
| **Closed** | 2026-05-11 |
| **Commit** | fc4722d |

## Description

Phase 5 of the remote-access plan (#0076), and the resolution of `RemoteAccess.md` Open Question 4. Today the inline thumbnail path uses `NSImage(contentsOf:)` synchronously — fine for a local file, broken for a remote attachment that hasn't arrived yet. Remote attachments need an async fetch with a placeholder, plus an actual streaming download for large media (the 4.5 MB `.mov` in #0072 is the realistic upper bound).

## Where to look

- **`Issues/Views/AttachmentThumbnailView.swift`** (or wherever inline thumbnails render — find via `grep -rn "NSImage(contentsOf:" Issues/`).
- **`Issues/Models/Issue.swift`** — `attachments: [String]` already exists. The remote source returns relative names; the local source returns absolute paths. The view layer needs a uniform way to load bytes from either.
- **New: a small `AttachmentLoader` actor** that takes `(IssueSource, attachmentName)` and returns `Data` (or an `Image`). Local source loads from disk; remote source uses `URLSession.bytes(for:)` against `/v1/folders/{id}/issues/{iid}/attachments/{name}` (#0081).

## Behavior

### Loading

```swift
actor AttachmentLoader {
    func load(name: String, from source: IssueSource) async throws -> Data
    func loadImage(name: String, from source: IssueSource) async throws -> NSImage
    var cache: [CacheKey: Data]   // small in-memory cache
}
```

- **Local source**: read the file off disk. No change from today's behavior except the call moves off the main thread.
- **Remote source**: stream via `URLSession.bytes(for:)`. Honor the `Content-Length` header to update progress. Decode the image once after bytes complete.
- **Cache**: keep decoded images in memory keyed by `(folderId, issueId, name)`. Evict on memory pressure (`NSCache` natively, or a manual LRU). Never persist to disk — the viewer is read-only and disk-cache-free per `RemoteAccess.md` non-goals.

### Quick Look path

For the existing Quick Look integration (referenced in `RemoteAccess.md` Open Q4 from #0073), the same `AttachmentLoader` materializes a temp file when Quick Look needs a URL. Write to `NSTemporaryDirectory()`, hand Quick Look the URL, delete on dismiss.

### UI

- Thumbnail view starts as a small placeholder (a generic file icon plus the filename), shows a determinate progress bar if `Content-Length > 256 KB`, replaces with the decoded image on success.
- On failure (404, network error), show an inline error glyph with a tap-to-retry; don't blank the row.
- Loading is per-issue; opening a different issue triggers a fresh load (the cache short-circuits if the bytes are already in memory).

## Verification

- Open a remote issue with a `.png` attachment → placeholder briefly, image renders within ~200 ms on LAN.
- Open #0072's 4.5 MB `.mov` over remote → progress bar shows, video preview becomes available within a couple of seconds on LAN, longer over Tailscale.
- Open the same attachment again on the next issue switch — cache hit, no network call.
- Toggle Wi-Fi off mid-load — error glyph appears with retry.
- Quick Look on a remote attachment → preview opens; closing it removes the temp file.

## Notes

- `URLSession.bytes(for:)` is the right primitive for streaming; `URLSession.shared.dataTask` would buffer the entire response into memory.
- Avoid concurrent fetches for the same `(folderId, issueId, name)` tuple — collapse to a single in-flight task with multiple awaiters. `AsyncSemaphore`-style or a simple `[CacheKey: Task<Data, Error>]` map.
- Don't move the local-attachment path off `NSImage(contentsOf:)` if it's already working — only the remote path needs the streaming fetch. Both can flow through the same `AttachmentLoader` interface, with different impls.

## Scope (out)

- Range requests / partial downloads on the server — not in #0081, not added here.
- On-disk caching — explicitly excluded by the read-only viewer model.
- Thumbnail size variants — the server doesn't generate them; the viewer scales after decode.

## Resolution

Shipped the load-bearing data path as `AttachmentLoader` + the wire glue; the view-layer rewire into `AttachmentThumbnailView` is a small follow-up that benefits from visual verification (left intentionally for the next pass to keep this commit testable end-to-end).

- **`Issues/Remote/AttachmentLoader.swift`** — `actor AttachmentLoader` with two correctness properties:
  - **Cache**. In-memory LRU keyed on `CacheKey(folderId, issueId, name)`. 32 MB budget — comfortable for the 4.5 MB MOV scenario from #0072 plus a half-dozen PNGs. `invalidate(folderId:issueId:)` is the integration point for #0102's `update`/`delete` events — when the host signals an issue changed, evict its attachments so the next render re-fetches. No disk cache (read-only viewer per `RemoteAccess.md` non-goals).
  - **In-flight dedup**. A `[CacheKey: Task<Data, Error>]` map collapses concurrent loads of the same key to one fetch; every awaiter sees the same result. Prevents a fast scroll past a remote image from spamming N parallel downloads.
- **`RemoteClient.fetchAttachmentData(folderId:issueId:name:)`** — added to the protocol + `URLSessionRemoteClient`. Uses `URLSession.bytes(for:)` for the streaming download per spec; reserves capacity off `Content-Length` so a known-size payload doesn't balloon allocations. 401 → `RemoteClientError.unauthorized`, 404 → `.notFound`.
- **`RemoteHostIssueSource.fetchAttachmentData(issueId:name:)`** — thin pass-through onto the source's bound client.

Local source path reads off a background `Task.detached(priority: .utility)` so the main thread doesn't block on a synchronous `Data(contentsOf:)`.

### Deferred for the follow-up

- **`AttachmentThumbnailView` rewire.** The existing inline thumbnail uses `NSImage(contentsOf:)` synchronously, which works for local sources but blocks rendering for remote attachments that haven't arrived. The view's path-detect (`issues-remote://` scheme) + async `task` binding to `AttachmentLoader.shared` is intentionally deferred because the visual transitions (placeholder → progress → image, error glyph + retry) benefit from real-eyes review.
- **Quick Look temp-file path.** When Quick Look needs a `URL` for a remote attachment, the loader can materialize a temp file via `AttachmentLoader.shared.load(...)` + `Data.write(to: NSTemporaryDirectory()…)`. The temp-file lifetime tracking lives with the QL view modifier.

## Files changed

- `Issues/Remote/RemoteClient.swift` — `fetchAttachmentData` protocol method + URLSession implementation.
- `Issues/Remote/RemoteHostIssueSource.swift` — pass-through helper.
- `Issues/Remote/AttachmentLoader.swift` — new actor.
- `IssuesTests/AttachmentLoaderTests.swift` — local-disk read, cache hit serves after on-disk delete, missing-file throws, invalidate drops the cached entry.
- `IssuesTests/RemoteHostIssueSourceTests.swift` — stub `RemoteClientProtocol` implements `fetchAttachmentData` (throws `.notFound`) so the test suite keeps compiling.

## Gotchas

- **`URLSession.bytes(for:)` not `.data(for:)`.** Buffer the full response by appending each `UInt8` to `Data`; `.data(for:)` would block on a single allocation for the full body. For a 32 MB cache budget the streaming path is a noticeable correctness improvement.
- **`Content-Length` is opportunistic.** If the host omits it (we don't, but a future hop might), the buffer just grows naturally. Don't `precondition` on it.
- **Cache eviction is LRU on byte budget, not entry count.** A single 30 MB MOV evicts a bunch of PNGs; one PNG never evicts the MOV. Matches the spec's "small in-memory cache" intent — the working set is what fits in 32 MB.
- **`invalidate` is the only correctness contract for staleness.** Without it, a host-side edit doesn't reach the viewer's cached image. The WS event wiring in `RemoteHostIssueSource.handleWebSocketEvent` already drops the cached BODY on `update`; the corresponding `AttachmentLoader.invalidate` call is the natural complement when the view layer wires up.
- **Loader follow-up.** When `AttachmentThumbnailView` adopts the loader, the view file is the right place to put the placeholder/progress/error UX. The loader stays pure of SwiftUI.
